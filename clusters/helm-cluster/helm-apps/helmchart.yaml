apiVersion: source.toolkit.fluxcd.io/v1beta1
kind: HelmChart
metadata:
  name: helmapp
  namespace: flux-system
spec:
  chart: ./charts/appchart
  sourceRef:
    name: flux-system
    kind: GitRepository
  interval: 10m

apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: helmapp
  namespace: flux-system
spec:
  interval: 5m
  chart:
    spec:
      chart: ./charts/appchart
      sourceRef:
        name: flux-system
        kind: GitRepository
      valuesFiles:
        - values.yaml
